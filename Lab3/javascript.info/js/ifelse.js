// let year = prompt('In which year was ECMAScript-2015 specification published?');

// if (year == 2015) alert("You are right!");
// else alert("Wrong!");

// let result = condition ? value1 : value2;

// let age = 20;

// let accessAllowed = (age > 18) ? true : false;

// let age = prompt('age?', 18);

// let message = (age < 3) ? 'Hi, baby!' :
//     (age < 18) ? 'Hello!' :
//     (age < 100) ? 'Greetings!' :
//     'What an unusual age!';

// alert(age);

// if ("0") {
//     alert( 'Hello' );
//   } // Hello, since string is not empty which is always true

// res = prompt('What is the "official" name of JavaScript?', '');

// let result = res == "ECMAScript" ? alert("Right!") : alert("You don't know?\nECMAScript");

// num = prompt("Enter number: ", '');

// if (num > 0) {
//     alert(1);
// } else if (num < 0) {
//     alert(-1);
// } else {
//     alert(0);
// }

let result = a + b < 4 ? 'Below' : 'Other';

let message = (login == 'Employee') ? 'Hello' :
    (login == 'Director') ? 'Greetings' :
    (login == '') ? 'No login' :
    '';

